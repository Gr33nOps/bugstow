# Public OAuth app IDs built into production builds (not secrets: they only
# identify the BugsTow app to Google and Dropbox; each user signs in with their
# own account). Registered for http://localhost:5757/oauth/callback, see
# docs/CLOUD_SYNC.md. A BugsTow server can override them at runtime with
# BUGSTOW_GOOGLE_CLIENT_ID / BUGSTOW_DROPBOX_APP_KEY (e.g. in bugstow.env).
VITE_GOOGLE_CLIENT_ID=1550385111-ddfs6f8tel6bpoa46d40e4e8qjepkmpl.apps.googleusercontent.com
VITE_DROPBOX_CLIENT_ID=r5tpf4li5uvcmvw
