{
  "name": "bugstow-server",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Bugstow self-hosted team server (Node + Express + SQLite + better-auth).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "reset-password": "tsx src/reset-password.ts",
    "setup-token": "tsx src/setup-token.ts",
    "decrypt-backup": "tsx src/decrypt-backup.ts",
    "test": "node --import tsx --test src/storage.test.ts src/backup.test.ts src/passwords.test.ts src/tls.test.ts src/http.test.ts src/cloudBackup.test.ts src/desktop.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-auth": "^1.7.5",
    "better-sqlite3": "^11.8.0",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "selfsigned": "^2.4.1",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
